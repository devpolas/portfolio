import { Project } from "./data/project-data"

export default function ProjectDialog({
  children,
  project,
}: {
  children: React.ReactNode
  project: Project
}) {
  return <section>Project Dialog</section>
}
